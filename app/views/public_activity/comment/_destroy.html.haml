.row.activity_alone
  %i.icon-trash
  %i.icon-comment
  %small
    = distance_of_time_in_words_to_now(activity.created_at)
    ,
    - if activity.trackable
      =link_to truncate(activity.trackable.name), project_path(activity.trackable.id)
    - else
      Comment was removed
